#!/bin/bash

unit () {
    nosetests tests/unit -c .noserc
}

acceptance () {
    python setup.py install
    nosetests tests/acception --with-spec --spec-color
}

clean () {
    find . -iname '*.pyc' -delete
}

if [ $1 ]; then
    $1 $2 $3 $4 $5;
else
    echo  "   acception - runner acceptance tests"
    echo  "   unit - runner unit tests"
    exit 1
fi;

